JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 1997 - 2004'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 SDI 68hp (50kW) 130Nm', 'Petrol', '1.9', 68
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 1997 - 2004'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 100hp (74kW) 240Nm', 'Diesel', '1.9', 100
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 1997 - 2004'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 115hp (85kW) 310Nm', 'Diesel', '1.9', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 1997 - 2004'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 130hp (96kW) 310Nm', 'Diesel', '1.9', 130
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 1997 - 2004'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 90hp (66kW) 210Nm', 'Diesel', '1.9', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 1997 - 2004'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 105hp (77kW) 175Nm', 'Petrol', '1.2', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 122hp (90kW) 200Nm', 'Petrol', '1.4', 122
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4i 16v 75hp (55kW) 126Nm', 'Petrol', '1.4', 75
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 FSI 115hp (85kW) 155Nm', 'Petrol', '1.6', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 105hp (77kW) 250Nm', 'Diesel', '1.6', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6i 8v 102hp (75kW) 148Nm', 'Petrol', '1.6', 102
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8 TSI 160hp (118kW) 250Nm', 'Petrol', '1.8', 160
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 105hp (77kW) 250Nm', 'Diesel', '1.9', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 FSI 150hp (110kW) 200Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 140hp (103kW) 320Nm', 'Diesel', '2.0', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 170hp (125kW) 350Nm', 'Diesel', '2.0', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 140hp (103kW) 320Nm', 'Diesel', '2.0', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 170hp (125kW) 350Nm', 'Diesel', '2.0', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TFSI RS 200hp (147kW) 280Nm', 'Petrol', '2.0', 200
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2004 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 TSI 115hp (85kW) 200Nm', 'Petrol', '1.0', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 105hp (77kW) 175Nm', 'Petrol', '1.2', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 86hp (63kW) 160Nm', 'Petrol', '1.2', 86
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 140hp (CHPA) (103kW) 250Nm', 'Petrol', '1.4', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 150hp (110kW) 250Nm', 'Petrol', '1.4', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI G-TEC 110hp (81kW) 200Nm', 'Petrol', '1.4', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI CR 105hp (77kW) 250Nm', 'Diesel', '1.6', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI CR 110hp (81kW) 250Nm', 'Diesel', '1.6', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI CR 90hp (66kW) 230Nm', 'Diesel', '1.6', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8 TSI 180hp (132kW) 250Nm', 'Petrol', '1.8', 180
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 150hp (110kW) 320Nm', 'Diesel', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 184hp (135kW) 380Nm', 'Diesel', '2.0', 184
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI RS 220hp (162kW) 350Nm', 'Petrol', '2.0', 220
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI RS Edition 230hp (169kW) 350Nm', 'Petrol', '2.0', 230
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 TSI 115hp (85kW) 200Nm', 'Petrol', '1.0', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 86hp (63kW) 160Nm', 'Petrol', '1.2', 86
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 140hp (CHPA) (103kW) 250Nm', 'Petrol', '1.4', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI G-tec 110hp (81kW) 200Nm', 'Petrol', '1.4', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSi 150hp (CHPB) (110kW) 250Nm', 'Petrol', '1.4', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 TSI 130hp (96kW) 200Nm', 'Petrol', '1.5', 130
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 TSI 150hp (110kW) 250Nm', 'Petrol', '1.5', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI CR 110hp (81kW) 250Nm', 'Diesel', '1.6', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI CR 115hp (85kW) 250Nm', 'Diesel', '1.6', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI CR 90hp (66kW) 230Nm', 'Diesel', '1.6', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8 TSI 180hp (132kW) 250Nm', 'Petrol', '1.8', 180
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 150hp (110kW) 320Nm', 'Diesel', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 184hp (135kW) 380Nm', 'Diesel', '2.0', 184
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI 190hp (140kW) 320Nm', 'Petrol', '2.0', 190
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI RS 230hp (169kW) 350Nm', 'Petrol', '2.0', 230
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI RS 245hp (180kW) 370Nm', 'Petrol', '2.0', 245
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2017 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 TSI 110hp (kW) Nm', 'Petrol', '1.0', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI iV 204hp (kW) Nm', 'Petrol', '1.4', 204
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 TSI 150hp (kW) Nm', 'Petrol', '1.5', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 115hp (85kW) 300Nm', 'Diesel', '2.0', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 150hp (110kW) 340Nm', 'Diesel', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 200hp (147kW) 400Nm', 'Diesel', '2.0', 200
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI 190hp (140kW) 320Nm', 'Petrol', '2.0', 190
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Octavia 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 TSi 110hp (81kW) 200Nm', 'Petrol', '1.0', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 TSi 95hp (70kW) 160Nm', 'Petrol', '1.0', 95
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 105hp (77kW) 175Nm', 'Petrol', '1.2', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 85hp (62kW) 160Nm', 'Petrol', '1.2', 85
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 90hp (66kW) 160Nm', 'Petrol', '1.2', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TDI 105hp (77kW) 250Nm', 'Diesel', '1.4', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TDI 75hp (55kW) 210Nm', 'Diesel', '1.4', 75
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TDI 90hp (66kW) 230Nm', 'Diesel', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 122hp (90kW) 200Nm', 'Petrol', '1.4', 122
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 125hp (92kW) 200Nm', 'Petrol', '1.4', 125
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 TDI 110hp (81kW) 250Nm', 'Diesel', '1.5', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 105hp (77kW) 250Nm', 'Diesel', '1.6', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 116hp (85kW) 250Nm', 'Diesel', '1.6', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 90hp (66kW) 230Nm', 'Diesel', '1.6', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Rapid 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2i 12v 65hp (48kW) 112Nm', 'Petrol', '1.2', 65
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2006 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TDI 70hp (51kW) 155Nm', 'Diesel', '1.4', 70
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2006 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TDI 80hp (59kW) 195Nm', 'Diesel', '1.4', 80
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2006 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4i 16v 86hp (63kW) 132Nm', 'Petrol', '1.4', 86
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2006 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6i 16v 105hp (77kW) 153Nm', 'Petrol', '1.6', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2006 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 105hp (77kW) 250Nm', 'Diesel', '1.9', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2006 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TDI 75hp (55kW) 180Nm', 'Diesel', '1.2', 75
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2010 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 105hp (77kW) 175Nm', 'Petrol', '1.2', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2010 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 86hp (63kW) 160Nm', 'Petrol', '1.2', 86
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2010 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2i 70hp (51kW) 112Nm', 'Petrol', '1.2', 70
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2010 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 105hp (77kW) 250Nm', 'Diesel', '1.6', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2010 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 90hp (66kW) 230Nm', 'Diesel', '1.6', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Roomster 2010 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 TSi 116hp (85kW) 200Nm', 'Petrol', '1.0', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Scala 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 TSi 95hp (70kW) 175Nm', 'Petrol', '1.0', 95
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Scala 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 TSI Edition S 190hp (kW) Nm', 'Petrol', '1.5', 190
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Scala 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 TSi 150hp (110kW) 250Nm', 'Petrol', '1.5', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Scala 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDi 115hp (85kW) 250Nm', 'Diesel', '1.6', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Scala 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8T 20v 150hp (110kW) 210Nm', 'Petrol', '1.8', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 100hp (74kW) 240Nm', 'Diesel', '1.9', 100
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 105hp (77kW) 250Nm', 'Diesel', '1.9', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 115hp (85kW) 285Nm', 'Diesel', '1.9', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 130hp (96kW) 285Nm', 'Diesel', '1.9', 130
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 136hp (100kW) 320Nm', 'Diesel', '2.0', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 140hp (103kW) 320Nm', 'Diesel', '2.0', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 170hp (125kW) 350Nm', 'Diesel', '2.0', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0i 8v 115hp (85kW) 172Nm', 'Petrol', '2.0', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 TDI V6 155hp (114kW) 310Nm', 'Diesel', '2.5', 155
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 TDI V6 163hp (120kW) 350Nm', 'Diesel', '2.5', 163
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.8 V6 193hp (142kW) 280Nm', 'Petrol', '2.8', 193
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 122hp (90kW) 200Nm', 'Petrol', '1.4', 122
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 140hp (103kW) 250Nm', 'Petrol', '1.4', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 105hp (77kW) 250Nm', 'Diesel', '1.6', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8 TSI 160hp (118kW) 250Nm', 'Petrol', '1.8', 160
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 TDI 105hp (77kW) 250Nm', 'Diesel', '1.9', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 140hp (103kW) 320Nm', 'Diesel', '2.0', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 170hp (125kW) 350Nm', 'Diesel', '2.0', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 150hp (110kW) 320Nm', 'Diesel', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 170hp (125kW) 350Nm', 'Diesel', '2.0', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 184hp (135kW) 380Nm', 'Diesel', '2.0', 184
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI 200hp (147kW) 280Nm', 'Petrol', '2.0', 200
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.6 FSI 260hp (191kW) 350Nm', 'Petrol', '3.6', 260
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 125hp (92kW) 200Nm', 'Petrol', '1.4', 125
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 150hp (110kW) 250Nm', 'Petrol', '1.4', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI CR 120hp (88kW) 280Nm', 'Diesel', '1.6', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8 TSI 180hp (132kW) 320Nm', 'Petrol', '1.8', 180
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 150hp (110kW) 340Nm', 'Diesel', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 190hp (140kW) 400Nm', 'Diesel', '2.0', 190
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI 220hp (162kW) 350Nm', 'Petrol', '2.0', 220
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI 280hp (206kW) 350Nm', 'Petrol', '2.0', 280
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI iV 218hp (160kW) 400Nm', 'Petrol', '1.4', 218
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 TSI 150hp (110kW) 250Nm', 'Petrol', '1.5', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 120hp (88kW) 250Nm', 'Diesel', '1.6', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 150hp (110kW) 320Nm', 'Diesel', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 190hp (140kW) 380Nm', 'Diesel', '2.0', 190
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 200hp (147kW) 400Nm', 'Diesel', '2.0', 200
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI 190hp (140kW) 320Nm', 'Petrol', '2.0', 190
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TSI 272hp (200kW) 350Nm', 'Petrol', '2.0', 272
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Superb 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 105hp (77kW) 175Nm', 'Petrol', '1.2', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2009 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 122hp (90kW) 200Nm', 'Petrol', '1.4', 122
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2009 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 105hp (77kW) 250Nm', 'Diesel', '1.6', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2009 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8 TSI 160hp (118kW) 250Nm', 'Petrol', '1.8', 160
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2009 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 110hp (81kW) 250Nm', 'Diesel', '2.0', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2009 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 140hp (103kW) 320Nm', 'Diesel', '2.0', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2009 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 170hp (125kW) 350Nm', 'Diesel', '2.0', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2009 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 105hp (77kW) 175Nm', 'Petrol', '1.2', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 TSI 110hp (81kW) 175Nm', 'Petrol', '1.2', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 122hp (90kW) 200Nm', 'Petrol', '1.4', 122
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 125hp (92kW) 200Nm', 'Petrol', '1.4', 125
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 TSI 150hp (110kW) 250Nm', 'Petrol', '1.4', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 TDI 105hp (77kW) 250Nm', 'Diesel', '1.6', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8 TSI 160hp (118kW) 250Nm', 'Petrol', '1.8', 160
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 110hp (81kW) 250Nm', 'Diesel', '2.0', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 140hp (103kW) 320Nm', 'Diesel', '2.0', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 150hp (110kW) 320Nm', 'Diesel', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI CR 170hp (125kW) 350Nm', 'Diesel', '2.0', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Skoda' AND vm.name = 'Yeti 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.8 CDI 45hp (33kW) 110Nm', 'Petrol', '0.8', 45
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'City 2002 - 2004'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Turbo 84hp (62kW) 120Nm', 'Petrol', '1.0', 84
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'City 2002 - 2004'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Turbo Brabus 98hp (72kW) 140Nm', 'Petrol', '1.0', 98
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'City 2002 - 2004'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.6 Turbo 70hp (51kW) 108Nm', 'Petrol', '0.6', 70
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'Crossblade 2002 - 2003'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 CDI 68hp (50kW) 160Nm', 'Petrol', '1.5', 68
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForFour 2004 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 DCI 95hp (70kW) 210Nm', 'Diesel', '1.5', 95
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForFour 2004 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 Turbo 177hp (130kW) 230Nm', 'Petrol', '1.5', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForFour 2004 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.9 Turbo 90hp (66kW) 135Nm', 'Petrol', '0.9', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForFour 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0i 61hp (45kW) 91Nm', 'Petrol', '1.0', 61
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForFour 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0i 71hp (52kW) 91Nm', 'Petrol', '1.0', 71
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForFour 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.8 CDI 41hp (30kW) 100Nm', 'Petrol', '0.8', 41
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.8 CDI 45hp (33kW) 110Nm', 'Petrol', '0.8', 45
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.8 CDI 54hp (40kW) 130Nm', 'Petrol', '0.8', 54
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.9 Turbo 90hp (66kW) 135Nm', 'Petrol', '0.9', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Turbo 84hp (62kW) 120Nm', 'Petrol', '1.0', 84
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Turbo Brabus 102hp (75kW) 147Nm', 'Petrol', '1.0', 102
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Turbo Brabus 98hp (72kW) 140Nm', 'Petrol', '1.0', 98
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0i 61hp (45kW) 92Nm', 'Petrol', '1.0', 61
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0i 71hp (52kW) 89Nm', 'Petrol', '1.0', 71
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, 'Ultimate 112 - 112hp (82kW) 150Nm', 'Petrol', '', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo -> 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.8 CDI 54hp (40kW) 130Nm', 'Petrol', '0.8', 54
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.9 Turbo 90hp (66kW) 135Nm', 'Petrol', '0.9', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.9 Turbo Brabus 109hp (80kW) 170Nm', 'Petrol', '0.9', 109
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0i 61hp (45kW) 92Nm', 'Petrol', '1.0', 61
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0i 71hp (52kW) 89Nm', 'Petrol', '1.0', 71
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'ForTwo 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.7 Turbo 62hp (46kW) 95Nm', 'Petrol', '0.7', 62
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'Roadster 2003 - 2006'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.7 Turbo 82hp (60kW) 110Nm', 'Petrol', '0.7', 82
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'Roadster 2003 - 2006'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '0.7 Turbo Brabus 101hp (74kW) 130Nm', 'Petrol', '0.7', 101
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Smart' AND vm.name = 'Roadster 2003 - 2006'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 Xdi 136hp (100kW) 310Nm', 'Petrol', '2.0', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Actyon All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 Xdi 141hp (104kW) 310Nm', 'Petrol', '2.0', 141
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Actyon All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 Xdi 155hp (114kW) 360Nm', 'Petrol', '2.0', 155
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Actyon All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 e-XDI 178hp (131kW) 400Nm', 'Petrol', '2.2', 178
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Actyon All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 XDi 150hp (110kW) 360Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Korando 2011 - 2018'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 XDi 175hp (129kW) 360Nm', 'Petrol', '2.0', 175
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Korando 2011 - 2018'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 e-XDI 178hp (131kW) 400Nm', 'Petrol', '2.2', 178
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Korando 2011 - 2018'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 T-GDI 163hp (kW) Nm', 'Petrol', '1.5', 163
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Korando 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 e-XDI 136hp (kW) Nm', 'Petrol', '1.6', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Korando 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '200 XDi 141hp (104kW) 310Nm', 'Petrol', '200', 141
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Kyron All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '270 XDi 165hp (121kW) 345Nm', 'Petrol', '270', 165
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Kyron All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 e-XDi 181hp (133kW) 420Nm', 'Petrol', '2.2', 181
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Musso 2018 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 XDi 155hp (114kW) 360Nm', 'Petrol', '2.0', 155
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Rexton -> 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 e-XDI 178hp (131kW) 400Nm', 'Petrol', '2.2', 178
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Rexton -> 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '270 XDi 163hp (120kW) 345Nm', 'Petrol', '270', 163
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Rexton -> 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '270 XDi 180hp (132kW) 402Nm', 'Petrol', '270', 180
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Rexton -> 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 e-XGDi 225hp (kW) Nm', 'Petrol', '2.0', 225
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Rexton 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 e-XDI 181hp (133kW) 420Nm', 'Petrol', '2.2', 181
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Rexton 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 XDi 155hp (114kW) 360Nm', 'Petrol', '2.0', 155
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Rodius All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 e-XDI 178hp (131kW) 400Nm', 'Petrol', '2.2', 178
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Rodius All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '270 XDi 165hp (121kW) 345Nm', 'Petrol', '270', 165
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Rodius All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 e-XDI 115hp (85kW) 300Nm', 'Petrol', '1.6', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Tivoli 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 T-GDI 128hp (kW) Nm', 'Petrol', '1.2', 128
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Tivoli 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 T-GDI 163hp (kW) Nm', 'Petrol', '1.5', 163
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Tivoli 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 e-XDI 136hp (kW) Nm', 'Petrol', '1.6', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'Tivoli 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 e-XDI 115hp (85kW) 300Nm', 'Petrol', '1.6', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'SsangYong' AND vm.name = 'XLV 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '101 - 101hp (74kW) 435Nm', 'Petrol', '101', 101
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '4100 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.4 - 112hp (82kW) 520Nm', 'Petrol', '4.4', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '4110 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.5 - 116hp (85kW) 520Nm', 'Petrol', '4.5', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '4115 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.4 - 121hp (89kW) 563Nm', 'Petrol', '4.4', 121
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '4120 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.4 - 132hp (97kW) 594Nm', 'Petrol', '4.4', 132
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '4130 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '116 - 116hp (85kW) 520Nm', 'Petrol', '116', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6115 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 - 117hp (86kW) 543Nm', 'Petrol', '6.7', 117
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6115 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 - 125hp (92kW) 566Nm', 'Petrol', '6.7', 125
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6125 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 - 126hp (93kW) 593Nm', 'Petrol', '6.7', 126
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6125 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT Ecotech Tier 4A - 131hp (96kW) 631Nm', 'Petrol', '6.7', 131
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6130 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 137hp (101kW) 570Nm', 'Petrol', '6.6', 137
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6135 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 - 136hp (100kW) 612Nm', 'Petrol', '6.7', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6135 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 141hp (104kW) 585Nm', 'Petrol', '6.6', 141
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6140 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 - 141hp (104kW) 613Nm', 'Petrol', '6.7', 141
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6140 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 147hp (108kW) 610Nm', 'Petrol', '6.6', 147
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6145 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT Ecotech Tier 4A - 146hp (107kW) 698Nm', 'Petrol', '6.7', 146
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6145 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '151 CVT - 151hp (111kW) 520Nm', 'Petrol', '151', 151
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6150 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 151hp (111kW) 630Nm', 'Petrol', '6.6', 151
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6150 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 156hp (115kW) 660Nm', 'Petrol', '6.6', 156
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6155 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 160hp (118kW) 675Nm', 'Petrol', '6.6', 160
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6160 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT Ecotech Tier 4A - 160hp (118kW) 767Nm', 'Petrol', '6.7', 160
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6160 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT - 167hp (123kW) 774Nm', 'Petrol', '6.7', 167
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6165 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 171hp (126kW) 725Nm', 'Petrol', '6.6', 171
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6170 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT Ecotech Tier 4A - 167hp (123kW) 825Nm', 'Petrol', '6.7', 167
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6170 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 175hp (129kW) 745Nm', 'Petrol', '6.6', 175
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6175 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT - 182hp (134kW) 844Nm', 'Petrol', '6.7', 182
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6180 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT Ecotech Tier 4A - 185hp (136kW) 863Nm', 'Petrol', '6.7', 185
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6185 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 192hp (141kW) 790Nm', 'Petrol', '6.6', 192
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6190 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.6 CVT - 196hp (144kW) 792Nm', 'Petrol', '6.6', 196
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6195 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT - 197hp (145kW) 860Nm', 'Petrol', '6.7', 197
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6200 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT Ecotech Tier 4A - 200hp (147kW) 927Nm', 'Petrol', '6.7', 200
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6205 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT Ecotech Tier 4A - 215hp (158kW) 995Nm', 'Petrol', '6.7', 215
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6215 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '6.7 CVT Ecotech Tier 4A - 227hp (167kW) 1089Nm', 'Petrol', '6.7', 227
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Steyr' AND vm.name = '6230 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0i 200hp (147kW) 205Nm', 'Petrol', '2.0', 200
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'BRZ 2012 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0i - 205hp (151kW) 211Nm', 'Petrol', '2.0', 205
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'BRZ 2017 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4i 231hp (kW) Nm', 'Petrol', '2.4', 231
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'BRZ 2021 -> ...'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 XT 177hp (130kW) 245Nm', 'Petrol', '2.0', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester -> 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 Turbo 210hp (154kW) 320Nm', 'Petrol', '2.5', 210
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester -> 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 Turbo 230hp (169kW) 320Nm', 'Petrol', '2.5', 230
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester -> 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D boxer 147hp (108kW) 350Nm', 'Petrol', '2.0', 147
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester 2008 - 2017'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 XT 240hp (176kW) 350Nm', 'Petrol', '2.0', 240
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester 2008 - 2017'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 Turbo 230hp (169kW) 320Nm', 'Petrol', '2.5', 230
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester 2008 - 2017'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8 Turbo 180hp (kW) Nm', 'Petrol', '1.8', 180
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester 2018 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D boxer 148hp (109kW) 350Nm', 'Petrol', '2.0', 148
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester 2018 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 XT 241hp (177kW) 350Nm', 'Petrol', '2.0', 241
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester 2018 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5i 181hp (kW) Nm', 'Petrol', '2.5', 181
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Forester 2018 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0T WRX 225hp (165kW) 300Nm', 'Petrol', '2.0', 225
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Impreza 2003 - 2005'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0T WRX STi 265hp (195kW) 340Nm', 'Petrol', '2.0', 265
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Impreza 2003 - 2005'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D Boxer 150hp (110kW) 350Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Impreza 2008 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0T 268hp (197kW) 350Nm', 'Petrol', '2.0', 268
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Impreza 2008 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5T WRX 230hp (169kW) 320Nm', 'Petrol', '2.5', 230
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Impreza 2008 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5T WRX STI 300hp (221kW) 407Nm', 'Petrol', '2.5', 300
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Impreza 2008 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D Boxer 150hp (110kW) 350Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Legacy 2008 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0i 150hp (110kW) 196Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Legacy 2008 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 170hp (125kW) 230Nm', 'Petrol', '2.5', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Legacy 2008 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5T GT 265hp (195kW) 350Nm', 'Petrol', '2.5', 265
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Legacy 2008 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D Boxer 150hp (110kW) 350Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Legacy 2015 - 2018'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.6i 256hp (188kW) 335Nm', 'Petrol', '3.6', 256
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Legacy 2015 - 2018'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6T GT 170hp (125kW) 250Nm', 'Petrol', '1.6', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Levorg 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 GT 150hp (110kW) 198Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Levorg 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 GT-S 268hp (197kW) 350Nm', 'Petrol', '2.0', 268
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Levorg 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0GT-S 295hp (217kW) 400Nm', 'Petrol', '2.0', 295
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Liberty 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.6VCT 260hp (191kW) 350Nm', 'Petrol', '3.6', 260
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Liberty 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D Boxer 150hp (110kW) 350Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Outback All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4 Turbo 260hp (kW) Nm', 'Petrol', '2.4', 260
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Outback All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5i 181hp (kW) Nm', 'Petrol', '2.5', 181
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Outback All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4D 90hp (66kW) 205Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Trezia 2011 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.6i 258hp (190kW) 350Nm', 'Petrol', '3.6', 258
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'Tribeca 2008 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0L 304hp (224kW) 422Nm', 'Petrol', '2.0', 304
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'WRX 2007 - 2014'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0T 286hp (210kW) 350Nm', 'Petrol', '2.0', 286
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'WRX 2015 - 2017'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 STi 300hp (221kW) 407Nm', 'Petrol', '2.5', 300
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'WRX 2015 - 2017'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D Boxer 150hp (110kW) 350Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'XV 2012 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0i 150hp (110kW) 197Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'XV 2012 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6i 114hp (84kW) 150Nm', 'Petrol', '1.6', 114
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'XV 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0i 156hp (115kW) 196Nm', 'Petrol', '2.0', 156
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Subaru' AND vm.name = 'XV 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Boosterjet 112hp (82kW) 170Nm', 'Petrol', '1.0', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Baleno 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, 'RS 1.0 Turbo 100hp (74kW) 150Nm', 'Petrol', '', 100
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Baleno 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2d 90hp (66kW) 200Nm', 'Petrol', '1.2', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Ciaz All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4i 92hp (68kW) 130Nm', 'Petrol', '1.4', 92
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Ciaz All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5d 95hp (70kW) 225Nm', 'Petrol', '1.5', 95
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Ciaz All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5i 105hp (77kW) 138Nm', 'Petrol', '1.5', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Ciaz All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TD 109hp (80kW) 250Nm', 'Petrol', '2.0', 109
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Grand Vitara 1999 - 2005'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TD 90hp (66kW) 205Nm', 'Petrol', '2.0', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Grand Vitara 1999 - 2005'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 TDI 109hp (80kW) 270Nm', 'Diesel', '2.0', 109
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Grand Vitara 1999 - 2005'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 DDiS 129hp (95kW) 300Nm', 'Petrol', '1.9', 129
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Grand Vitara 2005 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4i 169hp (124kW) 227Nm', 'Petrol', '2.4', 169
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Grand Vitara 2005 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 DDiS 129hp (95kW) 300Nm', 'Petrol', '1.9', 129
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Grand Vitara 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4i 169hp (124kW) 227Nm', 'Petrol', '2.4', 169
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Grand Vitara 2012 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.3 DDiS (51kW) 170Nm', 'Petrol', '1.3', NULL
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Ignis All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.3i 85hp (62kW) 110Nm', 'Petrol', '1.3', 85
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Jimny All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 D 65hp (48kW) 160Nm', 'Petrol', '1.5', 65
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Jimny All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 DDiS 86hp (63kW) 200Nm', 'Petrol', '1.5', 86
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Jimny All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5D 102hp (kW) Nm', 'Petrol', '1.5', 102
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Jimny All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4i 178hp (131kW) 230Nm', 'Petrol', '2.4', 178
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Kizashi 2010 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 DDiS 90hp (66kW) 200Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Liana All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Boosterjet 112hp (82kW) 170Nm', 'Petrol', '1.0', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 Boosterjet 140hp (103kW) 220Nm', 'Petrol', '1.4', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 DDiS 120hp (88kW) 320Nm', 'Petrol', '1.6', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 DDiS 90hp (66kW) 215Nm', 'Petrol', '1.6', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6i 107hp (79kW) 145Nm', 'Petrol', '1.6', 107
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6i 120hp (88kW) 156Nm', 'Petrol', '1.6', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.9 DDiS 120hp (88kW) 280Nm', 'Petrol', '1.9', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 DDiS 134hp (99kW) 320Nm', 'Petrol', '2.0', 134
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Boosterjet 110hp (81kW) 170Nm', 'Petrol', '1.0', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 S-Cross 2013 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 Boosterjet 140hp (103kW) 220Nm', 'Petrol', '1.4', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 S-Cross 2013 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 DDiS 120hp (88kW) 320Nm', 'Petrol', '1.6', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 S-Cross 2013 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6i 120hp (88kW) 156Nm', 'Petrol', '1.6', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'SX4 S-Cross 2013 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2i 86hp (63kW) 114Nm', 'Petrol', '1.2', 86
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Splash All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2i 94hp (69kW) 114Nm', 'Petrol', '1.2', 94
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Splash All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.3 DDiS 75hp (55kW) 190Nm', 'Petrol', '1.3', 75
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Splash All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.3 DDiS 69hp (51kW) 170Nm', 'Petrol', '1.3', 69
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2005 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 Sport 125hp (92kW) 148Nm', 'Petrol', '1.6', 125
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2005 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2i 12v 94hp (69kW) 118Nm', 'Petrol', '1.2', 94
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2011 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.3 DDiS 75hp (55kW) 190Nm', 'Petrol', '1.3', 75
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2011 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 VVT S 136hp (100kW) 160Nm', 'Petrol', '1.6', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2011 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Boosterjet 112hp (82kW) 170Nm', 'Petrol', '1.0', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 MHEV 90hp (kW) Nm', 'Petrol', '1.2', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2i 90hp (66kW) 120Nm', 'Petrol', '1.2', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 Boosterjet 140hp (103kW) 230Nm', 'Petrol', '1.4', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 Boosterjet MHEV 129hp (95kW) 235Nm', 'Petrol', '1.4', 129
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Swift 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 Boosterjet 140hp (103kW) 220Nm', 'Petrol', '1.4', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Vitara 2015 - 2017'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 DDIS 120hp (88kW) 320Nm', 'Petrol', '1.6', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Vitara 2015 - 2017'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6i 120hp (88kW) 156Nm', 'Petrol', '1.6', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Vitara 2015 - 2017'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 Boosterjet 112hp (82kW) 170Nm', 'Petrol', '1.0', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Vitara 2018 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 Boosterjet 140hp (103kW) 220Nm', 'Petrol', '1.4', 140
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Vitara 2018 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 Boosterjet MHEV 129hp (95kW) 235Nm', 'Petrol', '1.4', 129
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Vitara 2018 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.3 DDiS 69hp (51kW) 170Nm', 'Petrol', '1.3', 69
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'Wagon R All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.6 V6 252hp (185kW) 329Nm', 'Petrol', '3.6', 252
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Suzuki' AND vm.name = 'XL7 2007 - 2009'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '60 - 306hp (kW) Nm', 'Petrol', '60', 306
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '60 - 388hp (kW) Nm', 'Petrol', '60', 388
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '70 - 320hp (kW) Nm', 'Petrol', '70', 320
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '70D - 334hp (kW) Nm', 'Petrol', '70', 334
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '85 - 367hp (kW) Nm', 'Petrol', '85', 367
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '85D - 422hp (kW) Nm', 'Petrol', '85', 422
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '90D - 525hp (kW) Nm', 'Petrol', '90', 525
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, 'P85D - 700hp (kW) Nm', 'Petrol', '', 700
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, 'P90D - 762hp (kW) Nm', 'Petrol', '', 762
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2013 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '100D - 422hp (kW) Nm', 'Petrol', '100', 422
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '60 - 320hp (kW) Nm', 'Petrol', '60', 320
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '60D - 332hp (kW) Nm', 'Petrol', '60', 332
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '70 - 320hp (kW) Nm', 'Petrol', '70', 320
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '70D - 334hp (kW) Nm', 'Petrol', '70', 334
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '75 - 320hp (kW) Nm', 'Petrol', '75', 320
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '75D - 334p (kW) Nm', 'Petrol', '75', NULL
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '90D - 422hp (kW) Nm', 'Petrol', '90', 422
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, 'P100D - 612hp (kW) Nm', 'Petrol', '', 612
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, 'P90D - 469hp (kW) Nm', 'Petrol', '', 469
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model S 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '100D - 417hp (kW) Nm', 'Petrol', '100', 417
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model X 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '60D - 328hp (kW) Nm', 'Petrol', '60', 328
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model X 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '75D - 328hp (kW) Nm', 'Petrol', '75', 328
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model X 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '90D - 417hp (kW) Nm', 'Petrol', '90', 417
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model X 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, 'P100D - 612hp (kW) Nm', 'Petrol', '', 612
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model X 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, 'P90D - 463hp (kW) Nm', 'Petrol', '', 463
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Tesla' AND vm.name = 'Model X 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.0-V6 245hp (180kW) 380Nm', 'Petrol', '4.0', 245
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = '4Runner N280 - 2009 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4i 160hp (118kW) 220Nm', 'Petrol', '2.4', 160
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Alphard 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0i V6 220hp (162kW) 328Nm', 'Petrol', '3.0', 220
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Alphard 2002 - 2008'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4i 170hp (125kW) 235Nm', 'Petrol', '2.4', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Alphard 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.5i 280hp (206kW) 330Nm', 'Petrol', '3.5', 280
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Alphard 2008 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5i 180hp (132kW) 235Nm', 'Petrol', '2.5', 180
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Alphard 2015 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.5i 280hp (206kW) 330Nm', 'Petrol', '3.5', 280
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Alphard 2015 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.5i 300hp (221kW) 362Nm', 'Petrol', '3.5', 300
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Alphard 2015 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D4-D 90hp (66kW) 200Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Auris 2006 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D4-D 126hp (93kW) 310Nm', 'Petrol', '2.0', 126
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Auris 2006 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D4-D 128hp (94kW) 300Nm', 'Petrol', '2.0', 128
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Auris 2006 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D4-D 177hp (130kW) 400Nm', 'Petrol', '2.2', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Auris 2006 - 2012'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D4-D 90hp (66kW) 205Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Auris 2012 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 D4-D 112hp (82kW) 270Nm', 'Petrol', '1.6', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Auris 2012 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D4-D 124hp (91kW) 310Nm', 'Petrol', '2.0', 124
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Auris 2012 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2T 8v 116hp (85kW) 185Nm', 'Petrol', '1.2', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Auris 2015 - 2018'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 D4D 112hp (82kW) 270Nm', 'Diesel', '1.6', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Auris 2015 - 2018'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5i 203hp (kW) Nm', 'Petrol', '2.5', 203
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avelon 2015 - 2018'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.5i V6 268hp (kW) Nm', 'Petrol', '3.5', 268
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avelon 2015 - 2018'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5i 218hp (kW) Nm', 'Petrol', '2.5', 218
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avelon 2018 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.5i V6 301hp (kW) Nm', 'Petrol', '3.5', 301
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avelon 2018 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 116hp (85kW) 280Nm', 'Petrol', '2.0', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avensis 2003 - 2009'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 126hp (93kW) 300Nm', 'Petrol', '2.0', 126
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avensis 2003 - 2009'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D 150hp (110kW) 310Nm', 'Petrol', '2.2', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avensis 2003 - 2009'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D dcat 177hp (130kW) 400Nm', 'Petrol', '2.2', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avensis 2003 - 2009'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 124hp (91kW) 310Nm', 'Petrol', '2.0', 124
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avensis 2009 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D 150hp (110kW) 340Nm', 'Petrol', '2.2', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avensis 2009 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D dcat 177hp (130kW) 400Nm', 'Petrol', '2.2', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avensis 2009 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 D-4D 112hp (82kW) 270Nm', 'Petrol', '1.6', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avensis 2015 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 143hp (105kW) 320Nm', 'Petrol', '2.0', 143
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Avensis 2015 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0i 68hp (50kW) 93Nm', 'Petrol', '1.0', 68
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Aygo All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D-4D 55hp (40kW) 130Nm', 'Petrol', '1.4', 55
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Aygo All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2T 116hp (85kW) 185Nm', 'Petrol', '1.2', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'CH-R 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D-4D 90hp (66kW) 200Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2002 - 2007'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 116hp (85kW) 280Nm', 'Petrol', '2.0', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2002 - 2007'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 126hp (93kW) 300Nm', 'Petrol', '2.0', 126
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2002 - 2007'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D 136hp (100kW) 310Nm', 'Petrol', '2.2', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2002 - 2007'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D 150hp (110kW) 340Nm', 'Petrol', '2.2', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2002 - 2007'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D dcat 177hp (130kW) 400Nm', 'Petrol', '2.2', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2002 - 2007'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D-4D 90hp (66kW) 190Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2007 - 2011'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 126hp (93kW) 300Nm', 'Petrol', '2.0', 126
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2007 - 2011'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2 Turbo 116hp (85kW) 185Nm', 'Petrol', '1.2', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.8 Hybrid 122hp (kW) Nm', 'Hybrid', '1.8', 122
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 Hybrid 180hp (kW) Nm', 'Hybrid', '2.0', 180
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Corolla 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0T 235hp (173kW) 350Nm', 'Petrol', '2.0', 235
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Crown 2018 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4d 150hp (110kW) 400Nm', 'Petrol', '2.4', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Fortuner 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.8d 177hp (130kW) 420Nm', 'Petrol', '2.8', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Fortuner 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.0i V6 238hp (175kW) 376Nm', 'Petrol', '4.0', 238
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Fortuner 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0i 200hp (147kW) 205Nm', 'Petrol', '2.0', 200
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'GT86 2012 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0i - 205hp (151kW) 205Nm', 'Petrol', '2.0', 205
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'GT86 2016 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 D-4D 102hp (75kW) 260Nm', 'Petrol', '2.5', 102
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hiace All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 D-4D 117hp (86kW) 294Nm', 'Petrol', '2.5', 117
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hiace All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 D-4D 88hp (65kW) 192Nm', 'Petrol', '2.5', 88
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hiace All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.7 VVT-I 160hp (118kW) 240Nm', 'Petrol', '2.7', 160
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hiace All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 125hp (92kW) 315Nm', 'Petrol', '3.0', 125
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hiace All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 170hp (125kW) 360Nm', 'Petrol', '3.0', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hiace All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 Turbo 220hp (162kW) 350Nm', 'Petrol', '2.0', 220
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Highlander 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.7i 188hp (138kW) 225Nm', 'Petrol', '2.7', 188
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Highlander 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.5 V6 249hp (183kW) 356Nm', 'Petrol', '3.5', 249
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Highlander 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.5 V6 269hp (198kW) 358Nm', 'Petrol', '3.5', 269
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Highlander 2017 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 D-4D 102hp (75kW) 260Nm', 'Petrol', '2.5', 102
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2005 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 D-4D 120hp (88kW) 325Nm', 'Petrol', '2.5', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2005 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 D-4D 144hp (106kW) 343Nm', 'Petrol', '2.5', 144
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2005 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 163hp (120kW) 343Nm', 'Petrol', '3.0', 163
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2005 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 171hp (126kW) 360Nm', 'Petrol', '3.0', 171
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2005 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4 D-4D 150hp (110kW) 400Nm', 'Petrol', '2.4', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 D-4D 144hp (106kW) 343Nm', 'Petrol', '2.5', 144
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.8 D-4D 177hp (130kW) 420Nm', 'Petrol', '2.8', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 171hp (126kW) 360Nm', 'Petrol', '3.0', 171
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.0-VVTi 280hp (206kW) 375Nm', 'Petrol', '4.0', 280
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2015 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4d 150hp (110kW) 400Nm', 'Petrol', '2.4', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.8d 204hp (150kW) 500Nm', 'Petrol', '2.8', 204
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Hilux 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0 VVT-i 68hp (50kW) 91Nm', 'Petrol', '1.0', 68
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'IQ 2009 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.3 VVT-I 98hp (72kW) 125Nm', 'Petrol', '1.3', 98
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'IQ 2009 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D-4D 90hp (66kW) 190Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'IQ 2009 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 163hp (120kW) 343Nm', 'Petrol', '3.0', 163
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2002 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 166hp (122kW) 410Nm', 'Petrol', '3.0', 166
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2002 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 173hp (127kW) 410Nm', 'Petrol', '3.0', 173
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2002 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.0 V6 250hp (184kW) 382Nm', 'Petrol', '4.0', 250
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2002 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.2 TD 204hp (150kW) 430Nm', 'Petrol', '4.2', 204
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2002 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.5 D4-D 286hp (210kW) 650Nm', 'Petrol', '4.5', 286
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2002 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.7 V8 235hp (173kW) 434Nm', 'Petrol', '4.7', 235
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2002 - 2010'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 190hp (140kW) 420Nm', 'Petrol', '3.0', 190
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2010 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.0 V6 280hp (206kW) 360Nm', 'Petrol', '4.0', 280
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2010 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.8 D-4D 177hp (130kW) 450Nm', 'Petrol', '2.8', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2013 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 190hp (140kW) 420Nm', 'Petrol', '3.0', 190
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2013 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.5 D-4D V8 272hp (200kW) 650Nm', 'Petrol', '4.5', 272
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2013 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.6 VVTi 320hp (235kW) 460Nm', 'Petrol', '4.6', 320
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2013 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '5.7 VVTi 380hp (279kW) 545Nm', 'Petrol', '5.7', 380
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2013 - 2020'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.3 D-4D V6 309hp (kW) Nm', 'Petrol', '3.3', 309
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2021 -> ...'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.5 Twin Turbo V6 415hp (kW) Nm', 'Petrol', '3.5', 415
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Landcruiser 2021 -> ...'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 116hp (85kW) 250Nm', 'Petrol', '2.0', 116
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Previa All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 D-4D 90hp (66kW) 180Nm', 'Petrol', '1.6', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2014 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 128hp (94kW) 320Nm', 'Petrol', '2.0', 128
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2014 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 150hp (110kW) 370Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2014 - 2016'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 D-4D 102hp Euro6 (75kW) 270Nm', 'Petrol', '1.5', 102
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2016 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 D-4D 115hp Euro6 (85kW) 300Nm', 'Petrol', '1.6', 115
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2016 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 D-4D 95hp Euro6 (70kW) 210Nm', 'Petrol', '1.6', 95
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2016 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 120hp Euro6 (88kW) 340Nm', 'Petrol', '2.0', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2016 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 150hp Euro6 (110kW) 370Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2016 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 180hp Euro6 (132kW) 400Nm', 'Petrol', '2.0', 180
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2016 - 2019'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 D-4D 100hp (74kW) 270Nm', 'Petrol', '1.5', 100
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 D-4D 120hp (88kW) 300Nm', 'Petrol', '1.5', 120
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 122hp (90kW) 340Nm', 'Petrol', '2.0', 122
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 150hp (110kW) 370Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 177hp (130kW) 400Nm', 'Petrol', '2.0', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'ProAce 2020 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2T 110hp (81kW) 205Nm', 'Petrol', '1.2', 110
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Proace City 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.2T 130hp (96kW) 230Nm', 'Petrol', '1.2', 130
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Proace City 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 D-4D 100hp (74kW) 270Nm', 'Petrol', '1.5', 100
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Proace City 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.5 D-4D 130hp (96kW) 300Nm', 'Petrol', '1.5', 130
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Proace City 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 D-4D 102hp (75kW) 132Nm', 'Petrol', '2.5', 102
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Quantum All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.5 D-4D 88hp (65kW) 192Nm', 'Petrol', '2.5', 88
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Quantum All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.7 VVT-I 160hp (118kW) 240Nm', 'Petrol', '2.7', 160
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Quantum All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 125hp (92kW) 315Nm', 'Petrol', '3.0', 125
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Quantum All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0 D-4D 170hp (125kW) 360Nm', 'Petrol', '3.0', 170
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Quantum All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D 136hp (100kW) 310Nm', 'Petrol', '2.2', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Rav4 2005 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D 150hp (110kW) 340Nm', 'Petrol', '2.2', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Rav4 2005 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-Cat 177hp (130kW) 400Nm', 'Petrol', '2.2', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Rav4 2005 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.4 VVT-i 167hp (123kW) 224Nm', 'Petrol', '2.4', 167
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Rav4 2005 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 124hp (91kW) 310Nm', 'Petrol', '2.0', 124
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Rav4 2013 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 150hp (110kW) 340Nm', 'Petrol', '2.0', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Rav4 2013 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D4D 143hp (105kW) 320Nm', 'Diesel', '2.0', 143
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Rav4 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 VVT-i 152hp (112kW) 195Nm', 'Petrol', '2.0', 152
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Rav4 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0T 258hp (190kW) 400Nm', 'Petrol', '2.0', 258
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Supra 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.0T 340hp (250kW) 500Nm', 'Petrol', '3.0', 340
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Supra 2019 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.7i 159hp (117kW) 244Nm', 'Petrol', '2.7', 159
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Tacoma 2005 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.0i V6 236hp (174kW) 361Nm', 'Petrol', '4.0', 236
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Tacoma 2005 - 2015'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.7i 159hp (117kW) 244Nm', 'Petrol', '2.7', 159
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Tacoma 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '3.5 V6 278hp (204kW) 360Nm', 'Petrol', '3.5', 278
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Tacoma 2016 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.6L V8 310hp (228kW) 440Nm', 'Petrol', '4.6', 310
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Tundra 2015 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '5.7L V8 380hp (279kW) 540Nm', 'Petrol', '5.7', 380
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Tundra 2015 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D-4D 90hp (66kW) 190Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Urban Cruiser All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D 136hp (100kW) 310Nm', 'Petrol', '2.2', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Verso 2007 - 2009'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D dcat 177hp (130kW) 400Nm', 'Petrol', '2.2', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Verso 2007 - 2009'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D-4D 126hp (93kW) 310Nm', 'Petrol', '2.0', 126
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Verso 2009 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D 136hp (100kW) 340Nm', 'Petrol', '2.2', 136
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Verso 2009 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D 150hp (110kW) 340Nm', 'Petrol', '2.2', 150
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Verso 2009 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.2 D-4D dcat 177hp (130kW) 400Nm', 'Petrol', '2.2', 177
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Verso 2009 - 2013'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.6 D-4D 112hp (82kW) 270Nm', 'Petrol', '1.6', 112
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Verso 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '2.0 D4-D 126hp (93kW) 310Nm', 'Petrol', '2.0', 126
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Verso 2014 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D-4D 90hp (66kW) 205Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Verso S 2011 ->'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.0i 68hp (50kW) 90Nm', 'Petrol', '1.0', 68
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Yaris All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D-4D 55hp (40kW) 130Nm', 'Petrol', '1.4', 55
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Yaris All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D-4D 75hp (55kW) 180Nm', 'Petrol', '1.4', 75
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Yaris All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '1.4 D-4D 90hp (66kW) 190Nm', 'Petrol', '1.4', 90
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Yaris All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, 'GR 1.6T (kW) Nm', 'Petrol', '', NULL
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Toyota' AND vm.name = 'Yaris All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.4 HI - 105hp (77kW) 440Nm', 'Petrol', '4.4', 105
FROM vehicle_models vm
JOIN vehicle_brands vb ON vm.brand_id = vb.id
WHERE vb.name = 'Valtra' AND vm.name = '6400 All'
ON CONFLICT DO NOTHING;
INSERT INTO vehicle_engines (model_id, name, fuel_type, displacement, power_hp)
SELECT vm.id, '4.4 HI - 105hp (77kW) 440Nm', 'Petrol', '4.4', 105
FROM vehicle_models vm
